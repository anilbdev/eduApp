import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class RecentCourse extends Component {
    render() {
        return (
            <View>
                <Text> Recent Course </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
