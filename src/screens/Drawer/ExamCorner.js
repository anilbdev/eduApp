import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class ExamCorner extends Component {
    render() {
        return (
            <View>
                <Text> Exam Corner </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
