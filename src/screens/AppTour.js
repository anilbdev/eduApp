import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class AppTour extends Component {
    render() {
        return (
            <View>
                <Text> App tour page </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
